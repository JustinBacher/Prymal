[project]
requires-python = ">=3.10"

[tool.poetry]
name = "pryml"
version = "0.1.0"
description = ""
authors = ["Justin M Bacher"]
license = "LGPL V3.0"
readme = "README.md"

[tool.poetry.dependencies]
python = "<4.0,>=3.10"


[tool.poetry.group.dev.dependencies]
black = "^24.2.0"
isort = "^5.13.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
